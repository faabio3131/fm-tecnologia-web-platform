import type { Product } from "@/src/catalog/types"; import { formatBRL } from "@/src/catalog/commerce"; import { Badge } from "@/src/components/ui/badge"; import { ProductActions } from "./product-actions";
export function ProductPage({product}:{product:Product}){return <main><section className="product-hero"><div className="ambient ambient-one"/><div className="container narrow"><Badge>{product.publicLabel}</Badge><p className="product-code">FM / {product.id.toUpperCase()}</p><h1>{product.name}</h1><p className="lead">{product.positioning}</p><ProductActions product={product}/></div></section><section className="section"><div className="container split"><div><span className="eyebrow">Visão do produto</span><h2>Tecnologia com direção.</h2></div><p className="large-copy">{product.shortDescription} Esta página apresenta apenas informações atualmente aprovadas para divulgação.</p></div></section>{product.pricing&&<section className="section section--surface"><div className="container"><div className="pricing-summary"><div><span>Mensal</span><strong>{formatBRL(product.pricing.monthly)}<small>/mês</small></strong></div><div><span>Anual</span><strong>{formatBRL(product.pricing.annual)}<small>/ano</small></strong></div><div><span>Enterprise</span><strong>Sob consulta</strong></div></div>{product.trial&&<p className="trial-note">Teste por {product.trial.days} dias, sem cartão. {product.trial.constraint}</p>}</div></section>}<section className="section"><div className="container cta-panel"><span className="eyebrow">Próximo passo</span><h2>{product.lifecycle==="principal"?"Conheça uma nova forma de avançar.":"Acompanhe o que estamos construindo."}</h2><ProductActions product={product}/></div></section></main>}
