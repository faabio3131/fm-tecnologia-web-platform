import type { Product } from "@/src/catalog/types"; import { approvedCtaLabels,getProductActions } from "@/src/catalog/commerce"; import { ButtonLink } from "@/src/components/ui/button-link";
export function ProductActions({product}:{product:Product}){const a=getProductActions(product);if(!a.trial&&!a.specialist)return <p className="availability">Esta é uma iniciativa de tecnologia e P&amp;D, sem oferta comercial nesta etapa.</p>;return <div className="actions">{a.trial&&<ButtonLink href={`/contato?produto=${product.slug}&interesse=trial`}>{approvedCtaLabels.trial}</ButtonLink>}{a.specialist&&<ButtonLink href={`/contato?produto=${product.slug}`} variant="secondary">{approvedCtaLabels.specialist}</ButtonLink>}</div>}
