import { products } from "@/src/catalog/products"; import { formatBRL } from "@/src/catalog/commerce"; import { ContentPage } from "@/src/components/marketing/content-page"; import { ProductActions } from "@/src/components/marketing/product-actions"; import { createMetadata } from "@/src/lib/seo/metadata";
export const metadata=createMetadata("Preços","Planos aprovados de Kordena e Iron Fit.","/precos");
export default function Precos(){const priced=products.filter(p=>p.pricing);return <ContentPage eyebrow="Preços transparentes" title="Escolha o ponto de partida." intro="Condições comerciais aprovadas para os produtos principais da FM Tecnologia."><section className="section"><div className="container cards cards--two">{priced.map(p=><article className="price-card" key={p.id}><span>{p.publicLabel}</span><h2>{p.name}</h2><div className="price"><strong>{formatBRL(p.pricing!.monthly)}</strong><small>/mês</small></div><p>ou {formatBRL(p.pricing!.annual)}/ano</p><ul><li>Teste por 30 dias</li><li>Sem cartão</li>{p.trial?.constraint&&<li>{p.trial.constraint}</li>}<li>Enterprise sob consulta</li></ul><ProductActions product={p}/></article>)}</div></section><section className="section section--surface"><div className="container split"><div><span className="eyebrow">Enterprise</span><h2>Sob consulta.</h2></div><p className="large-copy">Converse com um especialista para avaliar o contexto da sua operação.</p></div></section></ContentPage>}
